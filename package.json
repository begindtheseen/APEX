{
  "name": "apex",
  "version": "1.0.0",
  "private": true,
  "description": "APEX — an offline-first PWA with four realms. Static site; this manifest exists only to carry the test tooling.",
  "scripts": {
    "test": "node test/run.js",
    "test:drift": "node test/drift.js",
    "test:browser": "node test/run.js --browser-only"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
